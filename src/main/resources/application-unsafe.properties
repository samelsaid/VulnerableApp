spring.datasource.admin.username=admin
spring.datasource.admin.password=hacker

spring.datasource.application.username=application
spring.datasource.application.password=hacker

# The H2 console is a full SQL client on /h2, and web-allow-others exposed it to every
# host that can reach the port rather than to localhost only. Both profiles keep it off;
# this profile used to switch it back on and override the public profile that disables it.
spring.h2.console.enabled=false
spring.h2.console.settings.web-allow-others=false

vulnerableapp.email.base-url=http://localhost
